Day Properties: 19/11/2025 
-------------------------- 
CSS:
---- 
	use to style an HTML document.
	describes how HTML elements should be displayed. 
	
Selector: 
--------- 
	Basic HTML Elements: 
	
	Example: 
	-------- 
		body {
			property_name_1: value_1; 
			property_name_2: value_2; 
			.
			.
			property_name_N: value_N; 
		} 
		
(5 - properties)
	color, background-color, font-family, font-size, text-align 
--------------------------------------------------------------- 

Day Properties: 20/11/2025 
-------------------------- 
CSS Syntax: 
----------- 
	A CSS rule consists of a selector and a declaration block
	
	Selector 
	{							
		color: blue; 		/* Declaration statement 1 (color - property, blue - value) */
		font-size: 12px; 	/* Declaration statement 2 */
	} 
	
Example: 
-------- 
	p 
	{
		color: blue; 
		font-size: 12px; 
	} 
	
Selector Types: 
--------------- 
	1. Simple selectors (select elements based on name, id, class) 
	2. Combinator selectors (select elements based on a specific relationship between them)
	3. Pseudo-class selectors (select elements based on a certain state)
	4. Pseudo-elements selectors (select and style a part of an element)
	5. Attribute selectors (select elements based on an attribute or attribute value) 

1. Simple Selector: 
------------------- 
	Universal Selector (*) - Selects all elements 
	Element Selector (element_name) - Selects all <p> elements 
	Class Selector (.class_name) - Selects all elements with match the class name 
	Specific Class Elements Selector(element_name.class_name). 
	Grouping Selector - Select all element1, element2, .., elementN. 
	
2. Combinator selector: 
----------------------- 
		select elements based on a specific relationship between them. 
		A combinator is a sign that(' ', '>', '+', '~', ',', '|') explains the relationship between selectors. 
		
		Types: 
		------
			1. Descendant Combinator (' ') -  selects all elements that are descendants (children, grandchildren, etc.) of a specified element.
			2. Child Combinator ('>') - selects all elements that are direct children of a specified element.
			3. next Sibling Combinator ('+') - select an element that is directly after a specific element. Sibling elements must have the same parent element.
			4. Subsequent-sibling combinator ('~') - selects all elements that are next siblings of a specified element. and share the same parent. 
			5. Selector-list Combinator (',') - select all the matching elements. Separate each element name with a comma. 

(5 - properties)
	text-decoration, padding, border, border-radius, margin
------------------------------------------------------------ 

Day Properties: 22/11/2025 
-------------------------- 

Pseudo Class Selector: 
----------------------
	pseudo-class is a keyword that can be added to a selector, to define a style for a special state of an element. 

Some common use for pseudo-classes:
----------------------------------- 
	Style an element when a user moves the mouse over it
	Style visited and unvisited links differently
	Style an element when it gets focus
	Style valid/invalid/required/optional form elements
	Style an element that is the first child of its parent

Syntax: 
------- 
	Pseudo-classes are always denoted by a single colon (:) followed by the pseudo-class name 

	selector:pseudo-class-name 
	{
		CSS properties
	} 

CSS Position: 
	position - ([default]static, relative, absolute, fixed). 

1. position: static (default)
	Every element is static unless you change it.
	The element follows normal document flow.
	Top/left/right/bottom have no effect. 

2. position: relative
	Still stays in normal document flow.
	BUT you can move it relative to its original position using top / left / right / bottom.
	It also becomes the positioning reference for absolutely positioned children.

3. position: absolute
	Removed from the normal flow.
	Positioned relative to the first ancestor that has position: relative, absolute, or fixed.
	If none exist, it positions relative to the viewport.

4. position: fixed
	Stays fixed to the viewport, even during scroll.
	Removed from normal flow.
	Always stays at the same place on the screen. 

| Position     | In Flow? | Moves With Scroll? | Positioned Relative To      |
| ------------ | -------- | ------------------ | --------------------------- |
|   static     | ✔ yes    | ✔ yes             | normal flow                 |
|   relative   | ✔ yes    | ✔ yes             | its original position       |
|   absolute   | ❌ no    | ✔ yes             | nearest positioned ancestor |
|   fixed      | ❌ no    | ❌ no             | viewport                    |


(5 - properties)
	width, height, opacity, display, cursor
-------------------------------------------

(5 - properties)
	line-height, letter-spacing, text-transform, visibility, overflow
--------------------------------------------------------------------- 

Day Properties: 23/11/2025 
-------------------------- 

Outline in CSS: 
--------------- 
	outline-style - Set the style of an outline 
	outline-width - Set the width of an outline 
	outline-color - Set the color of an outline 
	outline - A shorthand property for setting outline-width, outline-style, and outline-color in one declaration 
	outline-offset - Specifies the space between an outline and the edge or border of an element 

Text Formatting 
--------------- 
	vertical-align 
	--------------
		used to control the vertical positioning of an inline or inline-block element relative to its line box or 
		table-cell container. It does not work on block-level elements by default. 

Text Shadow 
----------- 
	text-shadow 
	----------- 
		adds shadow to text. In its simplest use, you only specify the horizontal and the vertical shadow. 
		In addition, you can add a shadow color and blur effect. 

		The text-shadow property also accepts multiple shadows for an element. Define each shadow in a comma-separated list.
		Example: text-shadow: 0 0 3px #ff0000, 0 0 5px #0000ff; 

font-weight: 
------------ 
	normal - This is default. Defines normal characters
	bold - Defines thick characters
	bolder - Defines thicker characters
	lighter - Defines lighter characters
	100-900 - Defines from thin to thick characters. 400 is the same as normal, and 700 is the same as bold 

------------------------------------------------------------------------------------------------------------ 

Day Properties: 24/11/2025 
-------------------------- 
Icons
----- 
	Icons can easily be added to your HTML page, by using an icon library.

How To Add Icons 
---------------- 
	The simplest way to add an icon to your HTML page, is with an icon library, such as CSS Bootstrap library. 
	Add the name of the specified icon class to any inline HTML element (like <i> or <span>). 
	Icons can be customized with CSS (size, color, shadow, etc.) 

Bootstrap Icons
--------------- 
	To use the Bootstrap glyphicons, add the following line inside the <head> section of your HTML page 
	<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css"> 

	Example: 
	-------- 
		<i class="glyphicon glyphicon-cloud"></i>
		<i class="glyphicon glyphicon-remove"></i>

CSS list
-------- 
	list-style-type - Specifies the type of list-item marker (ul[{default value}disc, circle, square, none] - ol[{default value}decimal, lower-alpha, upper-alpha, lower-roman, upper-roman]) 
	list-style-image - Specifies an image as the list-item marker
	list-style-position - Specifies the position of the list-item markers
	list-style - A shorthand property for the properties above 

CSS Table 
--------- 
	border-collapse - used in table element.possible values are (default value)separate, collapse.  
	border-spacing - sets the distance between the borders of adjacent cells. Note: This property works only when border-collapse is set to "separate".
	vertical-align - sets the vertical alignment (like top, bottom, or middle) of the content in <th> or <td>. Note: By default, the vertical alignment of the content in a table is middle (for both <th> and <td> elements). 

Width - Properties 
------------------ 

width 
----- 
	Sets the actual width of an element.
	Can be a fixed value (200px), percentage (50%), or auto. 

	Example: 
		div 
		{
			width: 300px;
		}

	The element will be exactly 300px wide, unless min-width or max-width override it. 

max-width 
--------- 
	Sets the largest width an element is allowed to grow to.
	Even if the parent container is very wide, the element will not exceed this value. 

	Example: 
	div 
	{
		max-width: 500px;
	}

	The element will never be wider than 500px. 

min-width 
--------- 
	Sets the smallest width an element is allowed to shrink to. 
	Even if content is small or the screen is narrow, the width cannot go below this value. 

	Example: 
	div 
	{
		min-width: 200px;
	} 

	The element will never be smaller than 200px, even if width is smaller. 
------------------------------------------------------------------------------ 

Day Properties: 25/11/2025 
-------------------------- 
float
----- 
	specifies how an element should float within its container. 
	It places an element on the left or right side of its container, allowing text and inline elements to wrap around it. 
	It is used for positioning and formatting content e.g. let an image float left to the text in a container. 
	It is often used to wrap text around images! 

	Possible Values: 
	---------------- 
		left - The element floats to the left of its container 
		right - The element floats to the right of its container 
		none - Default. The element does not float and is displayed just where it occurs in the text 
		inherit - The element inherits the float value of its parent 

clear 
----- 
	The clear property specifies what should happen with the element that is NEXT to a floating element.
	The clear property prevents elements from wrapping around or beside the floated content. 

	Possible Values: 
	---------------- 
		none - Default. Allows elements to float on either side
		left - The element is pushed below floated elements on its left side
		right - The element is pushed below floated elements, on its right side
		both - The element is pushed below floated elements, on both its left and right side
		inherit - The element inherits the clear value from its parent 

CSS Pseudo Elements 
------------------- 
	pseudo-element is a keyword that can be added to a selector, to style a specific part of an element. 

Some common use for pseudo-elements
----------------------------------- 
	Style the first letter or first line, of an element
	Insert content before or after an element
	Style the markers of list items
	Style the user-selected portion of an element
	Style the viewbox behind a dialog box 

Syntax 
------ 
	Pseudo-elements are denoted by a double colon (::) followed by the pseudo-element name 

	selector::pseudo-element-name 
	{
		CSS properties
	} 

::first-line 
------------ 
	Used to add a special style to the first line of a text. 
	It can only be applied to block-level elements. 

::first-letter 
-------------- 
	Used to add a special style to the first letter of a text. 
	It can only be applied to block-level elements. 

::before 
-------- 
	Used to insert some content before the content of a specified element.
	Use the "content" property to specify the content to insert. 

::after 
------- 
	 Used to insert some content after the content of a specified element. 
	 Use the content property to specify the content to insert.  

Dialog Box 
---------- 
	A dialog box is a small window that appears on top of a webpage (or application) 
	to communicate something to the user or ask for input. 
	It usually pauses the main content until the user interacts with it.

	In web development, dialog boxes are often used for:
	---------------------------------------------------- 
		Confirmations (e.g., “Are you sure you want to delete?”)
		Forms (login, signup, feedback)
		Alerts or messages
		Custom pop-up interfaces 

In simple words
--------------- 
	A dialog box = a pop-up window used to interact with the user.

HTML Dialog Box
--------------- 
	In HTML, a dialog box is typically created using the <dialog> element.

	Example: 
	-------- 
		<dialog id="box">
			<p>This is a dialog box!</p>
			<button onclick="box.close()">Close</button>
		</dialog>

		<button onclick="box.showModal()">Open Dialog</button>
Types: 
	Modal = “Stop everything and deal with me first.”
	Non-modal = “I’m here, but you can keep doing whatever you want.” 

| Feature                 | Modal Dialog                   | Non-Modal Dialog      |
| ----------------------- | ------------------------------ | --------------------  |
| Blocks page interaction | ✔ Yes                          | ✖ No                 |
| User must close it      | ✔ Yes                          | ✖ No                 |
| Focus trapping          | ✔ Yes                          | ✖ No                 |
| Use cases               | Confirmations, login, warnings | Tools, notifications  |
| Open method             | showModal()                    | show()                | 

1. Modal Dialog
---------------
	A modal dialog is a dialog window that blocks interaction with the rest of the page until the user closes it. 

Key Characteristics 
------------------- 
	The user cannot click, scroll, or interact with anything outside the dialog.
	Focus (tab navigation) is trapped inside the dialog.
	Ideal when you need the user’s attention for something important. 

How to open a modal dialog
-------------------------- 
	dialog.showModal();

Example of a Modal Dialog
------------------------- 

<dialog id="modalDialog">
	<h3>Important Message</h3>
  	<p>You must confirm this action before proceeding.</p>
  	<button onclick="modalDialog.close()">OK</button>
</dialog>

<button onclick="modalDialog.showModal()">Open Modal Dialog</button>

When to use a Modal Dialog
-------------------------- 
	Delete confirmation
	Login popup
	Critical warnings
	Required form input
	Anything where the user must respond before continuing 

Why modal dialogs are important 
------------------------------- 
	They prevent accidental clicks outside the popup and improve accessibility by keeping keyboard focus inside the dialog.  

2. Non-Modal Dialog
------------------- 
	A non-modal dialog is a popup that does NOT block interaction with the rest of the webpage. 

Key Characteristics
------------------- 
	The user can continue interacting with the page behind the dialog.
	The dialog behaves like a floating window.
	Focus is not trapped—user can click anywhere.

How to open a non-modal dialog 
------------------------------ 
	dialog.show();

Example of a Non-Modal Dialog 
----------------------------- 

<dialog id="nonModalDialog">
  	<h3>Notification</h3>
  	<p>You can still interact with the page behind this dialog.</p>
	<button onclick="nonModalDialog.close()">Close</button>
</dialog>

<button onclick="nonModalDialog.show()">Open Non-Modal Dialog</button>

When to use a Non-Modal Dialog
------------------------------ 
	Chat windows (floating)
	Tool panels (like photo editor side windows)
	Small tips or hints
	Information popups that don’t require user action 

---------------------------------------------------------------------------------------- 

Day Properties: 26/11/2025 
-------------------------- 

@media 
------ 

Meaning of only screen
---------------------- 
	screen = apply styles to screens (desktops, laptops, tablets, phones). 
	only = only = a filter that prevents older browsers from applying the styles. 

Full example:
------------- 
	@media only screen and (max-width: 768px) 
	{
		/* styles */
	}

Why do we use only?
------------------- 
	In modern browsers, using only is optional. 
	It mainly existed to prevent old browsers (IE8 and earlier) from misinterpreting the media query. 


Without only
------------  
	@media screen and (max-width: 768px) { ... }

With only 
--------- 
	@media only screen and (max-width: 768px) { ... }

Functionally, they work the same in all current browsers.

What does screen mean?
---------------------- 
	It tells the browser:
		“Apply these styles to screen-based devices” 
		—not to printers, TVs, projectors, etc. 

Other media types include:
-------------------------- 
	| Media Type | Meaning                   |
	| ---------- | ------------------------- |
	| `screen`   | Phones, tablets, monitors |
	| `print`    | Printed/PDF documents     |
	| `speech`   | Screen readers            |

Summary
------- 
	| Part       | Meaning                                   | Usefulness today   |
	| ---------- | ----------------------------------------- | ------------------ |
	| screen     | Targets display devices                   | Commonly used      |
	|   only     | Prevents old browsers from applying rules | Not required today |

Final Answer: 
------------- 
	only screen means:
		“Apply this media query only on screen devices, and ignore it in old browsers.”

Attribute Selector 
------------------  
	used to select and style HTML elements with a specific attribute or attribute value, or both. 

[attribute]			Select elements with a specific attribute
[attribute=value]	Select elements with a specific attribute with an exact value
[attribute~=value]	Select elements with an attribute value containing a specific word
[attribute|=value]	Select elements with the specific attribute, whose value can be exactly the specific value, or start with the specific value followed by a hyphen (-)
[attribute^=value]	Select elements with the specific attribute, whose value starts with a specific value
[attribute$=value]	Select elements whose attribute value ends with a specific value
[attribute*=value]	Select elements whose attribute value contains a specific value 
 
Day Properties: 28/11/2025 
-------------------------- 

CSS Flexbox 
-----------
	CSS Flexbox is used for a one-dimensional layout, with rows OR columns.
	Flexbox makes it easy to design a flexible and responsive layout, without using float or positioning. 

CSS Flexbox Components
---------------------- 
	Flex Container: 
	--------------- 
		A Flex Container - The parent (container) element, where the display property is set to flex or inline-flex. 

	Flex Items: 
	-----------
		One or more Flex Items - The direct children of the flex container automatically becomes flex items. (or) The direct child elements of a flex container automatically becomes flex items. 

=== 

Flex Container Properties 
------------------------- 
	display - Must be set to "flex" or "inline-flex" 
	
	flex-direction - Sets the display-direction of flex items 
	flex-wrap - Specifies whether the flex items should wrap or not 
	flex-flow - Shorthand property for "flex-direction" and "flex-wrap" 

	justify-content - Aligns the flex items when they do not use all available space on the main-axis (horizontally) 
	align-items - Aligns the flex items when they do not use all available space on the cross-axis (vertically) 

	align-content - Aligns the flex lines when there is extra space in the cross axis and flex items wrap 

flex-direction: 
	specifies the display-direction of flex items in the flex container. 
	Values: row (default), column, row-reverse, column-reverse 

flex-wrap: 
	specifies whether the flex items should wrap or not, if there is not enough room for them on one flex line. 
	Values: nowrap (default), wrap, wrap-reverse 

flex-flow: 
	The flex-flow property is a shorthand property for setting both the flex-direction and flex-wrap properties. 
	Example: flex-flow: row wrap; 

justify-content: 
	Used to align the flex items when they do not use all available space on the main-axis (horizontally). 
	Values: flex-start (default), flex-end, center, space-around, space-between, space-evenly

align-items: 
	Used to align the flex items when they do not use all available space on the cross-axis (vertically).
	Values: normal (default), flex-start, flex-end, center, stretch, baseline 

align-content: 
	The align-content property is similar to align-items, but instead of aligning the flex items, it aligns the flex lines.
	Values: stretch (default), flex-start, flex-end, center, space-around, space-between, space-evenly 

Flex Item Properties: 
--------------------- 
	order 		- Specifies the display order of the flex items inside the flex container
	
	flex-grow 	- Specifies how much a flex item will grow relative to the rest of the flex items
	flex-shrink - Specifies how much a flex item will shrink relative to the rest of the flex items
	flex-basis 	- Specifies the initial length of a flex item
	flex 		- Shorthand property for flex-grow, flex-shrink, and flex-basis
	
	align-self 	- Specifies the alignment for the flex item inside the flex container 

order: 
------ 
	specifies the display order of the flex items inside the flex container.
	The first flex item in the source code does not have to appear as the first item in the layout.
	The order value must be a number, and the default value is 0. 

flex-grow: 
---------- 
	specifies how much a flex item will grow relative to the rest of the flex items. 
	The value must be a number, and the default value is 0. 

flex-shrink: 
------------ 
	specifies how much a flex item will shrink relative to the rest of the flex items.
	The value must be a number, and the default value is 1. 

flex-basis: 
----------- 
	specifies the initial length of a flex item. 

flex: 
----- 
	The flex property is a shorthand property for the "flex-grow", "flex-shrink", and "flex-basis" properties.

	Example: 
	--------
		Make the third flex item growable (1), not shrinkable (0), and with an initial length of 150 pixels

		<div class="flex-container">
			<div>1</div>
			<div>2</div>
			<div style="flex: 1 0 150px">3</div>
			<div>4</div>
		</div>

align-self: 
----------- 
	specifies the alignment for the selected item inside the flexible container.
	This property overrides the default alignment set by the container's align-items property.
	
------------------------------------------------------------------------------------------------ 

Day Properties: 29/11/2025 
-------------------------- 
	
CSS Grid Layout Module
---------------------- 
	The Grid Layout Module offers a grid-based layout system, with rows and columns.
	The Grid Layout Module allows developers to easily create complex web layouts.
	The Grid Layout Module makes it easy to design a responsive layout structure, without using float or positioning. 

Grid vs. Flexbox
---------------- 
	CSS Grid is used for two-dimensional layout, with rows AND columns.
	CSS Flexbox is used for one-dimensional layout, with rows OR columns. 

CSS Grid Components
------------------- 
	A Grid Container: 
		 The parent (container) element, where the display property is set to grid or inline-grid 
	
	One or more Grid Items: 
		The direct children of the grid container automatically becomes grid items

All CSS Grid Properties: 
------------------------ 
Property				Description







grid-column-start		Specifies where to start the grid item
grid-column-end			Specifies where to end the grid item 
grid-column				A shorthand property for the grid-column-start and the grid-column-end properties 

grid-row-start			Specifies where to start the grid item
grid-row-end			Specifies where to end the grid item 
grid-row				A shorthand property for the grid-row-start and the grid-row-end properties 

grid-area				Either specifies a name for the grid item, or this property is a shorthand property for the grid-row-start, grid-column-start, grid-row-end, and grid-column-end properties





align-self				Aligns the content for a specific grid item along the column axis
justify-self			Aligns the content for a specific grid item along the row axis
place-self				A shorthand property for the align-self and the justify-self properties 

=== 

grid container properties
-------------------------
display					Specifies the display behavior (the type of rendering box) of an element 

grid-template-rows		Specifies the size of the rows in a grid layout
grid-template-columns	Specifies the size of the columns, and how many columns in a grid layout 
grid-template-areas		Specifies how to display columns and rows, using named grid items
grid-template			A shorthand property for the grid-template-rows, grid-template-columns and grid-template-areas properties

row-gap					Specifies the gap between the grid rows
column-gap				Specifies the gap between the columns
gap						A shorthand property for the row-gap and the column-gap properties 

grid-template-rows		Specifies the size of the rows in a grid layout
grid-template-columns	Specifies the size of the columns, and how many columns in a grid layout 
grid-template-areas		Specifies how to display columns and rows, using named grid items
grid-auto-rows			Specifies a default row size 
grid-auto-columns		Specifies a default column size 
grid-auto-flow			Specifies how auto-placed items are inserted in the grid 
grid					A shorthand property for the grid-template-rows, grid-template-columns, grid-template-areas, grid-auto-rows, grid-auto-columns, and the grid-auto-flow properties 

align-content			Vertically aligns the whole grid inside the container (when total grid size is smaller than container)
justify-content			Horizontally aligns the whole grid inside the container (when total grid size is smaller than container)
place-content			A shorthand property for the align-content and the justify-content properties 

align-items				Specifies the default alignment for items inside a flexbox or grid container

=== 

display 
	The <div> element becomes a grid container when its display property is set to grid or inline-grid. 

grid-template-columns 
	defines the number and width of the columns in the grid. 
	The value is a space-separated-list, where each value defines the width of the respective column. 

	Common values are:
		Fixed lengths (100px 300px 200px)
		Percentages (20% 60% 20%)
		fr unit (1fr 2fr 1fr)
		auto (auto auto auto)
		repeat() (repeat(3, 1fr))
		minmax() (minmax(80px, 1fr) 150px 150px) 

	To create a grid layout with 3 columns of equal width, use the "auto" keyword.
	------------------------------------------------------------------------------
		.container {
			display: grid;
			grid-template-columns: auto auto auto;
		} 

	To create a grid layout with 3 columns: 2 columns with a fixed width, and 1 column with size "auto".
	------------------------------------------------------------------------------ --------------------- 
		.container {
			display: grid;
			grid-template-columns: 80px 200px auto;
		}

	Note: If you have more than 3 grid items in a 3 columns grid, the grid will automatically add new rows to put the items in. 

	Grid Columns with the fr Unit
	----------------------------- 
		The fr unit stands for "fraction". This unit automatically divides the available space into fractions.
		1fr will take 1 fraction of the available space, while 2fr will take 2 fractions of the available space. 

		.container {
			display: grid;
			grid-template-columns: 1fr 1fr 1fr;
		}
	Grid Columns with the repeat() Function
	--------------------------------------- 
		The CSS repeat() function is used to repeat a set of columns or rows in a grid. 

		Use repeat() to create three equal-width columns in a grid
		.container {
			display: grid;
			grid-template-columns: repeat(3, 1fr);
		}

	Grid Columns with the minmax() Function
	---------------------------------------
		The CSS minmax() function is used to define a size-range greater than or equal to a min value and 
		less than or equal to a max value. 

		Use minmax() to define that first column is at least 80px wide and can grow to 1fr:
		.container {
			display: grid;
			grid-template-columns: minmax(80px, 1fr) 150px 150px;
		} 

grid-template-rows	
	The grid-template-rows property defines the number and height of the rows in the grid.
	The value is a space-separated-list, where each value defines the height of the respective row.

	Common values are:
		Fixed lengths
		Percentages
		fr unit
		auto
		min-content
		max-content
		repeat()
		minmax()
		fit-content()

	Example: 
		Set the first row in the grid to 80px high, and the second row to 200px high 
		.container {
			display: grid;
			grid-template-rows: 80px 200px;
		}

CSS Grid Gaps 
------------- 
	The space between the rows and columns in a grid container are called gaps (or gutters).
	The gaps are created between the grid rows and columns, not on the outer edges of the grid container. 
	The default size of the gap is 0, which means that there are no spacing between grid items. 

column-gap
	The column-gap property specifies the gap between the columns in a grid.

	Specify a 50 pixels gap between the grid columns:
	.container {
		display: grid;
		column-gap: 50px;
	} 

row-gap
	The row-gap property specifies the gap between the rows in a grid. 

	Specify a 50 pixels gap between the grid rows: 
	.container {
		display: grid;
		row-gap: 50px;
	}

gap 
	The gap property is a shorthand property for "row-gap" and "column-gap".
	If a single value is provided, it applies the same gap to both rows and columns.
	If two values are provided, the first value sets the row-gap, and the second value sets the column-gap.

	Using one value - Set gap between rows and columns to 50px:
	.container {
		display: grid;
		gap: 50px;
	} 

	Using two values - Set gap between rows to 30px, and gap between columns to 100px:
	.container {
		display: grid;
		gap: 30px 100px;
	}

CSS Align Grid Content 
---------------------- 

justify-content 
	Aligns the grid content when it does not use all available space on the main-axis (horizontally)
	Values: space-evenly, space-around, space-between, center, start, end 

	Note: The grid content's total width has to be less than the container's width for the justify-content property to have any effect. 

	The space-evenly value displays the grid items with equal space around them 
	.container {
		display: grid;
		grid-template-columns: 70px 70px 70px;
		background-color: dodgerblue;
		padding: 5px;
		gap: 5px;
		justify-content: space-evenly;
	} 

align-content 
	Aligns the grid content when it does not use all available space on the cross-axis (vertically) 
	Values: space-evenly, space-around, space-between, center, start, end 

	Note: The grid content's total height has to be less than the container's height for the align-content property to have any effect. 

	The center value positions the grid items in the middle of the container: 
	.container {
		display: grid;
		height: 300px;
		align-content: center;
	 } 

place-content 
	The place-content property is a shorthand property for the align-content and the justify-content properties.

	If place-content has two values:
		place-content: start center; - the align-content value is 'start' and justify-content value is 'center'

		The end space-between value aligns the grid lines towards the bottom of the grid container, and aligns the grid items with the same space between them horizontally 
		.container {
			display: grid;
			height: 300px;
			place-content: end space-between;
		}

	If place-content has one value:
		place-content: end; - both align-content and justify-content values are 'end' 

		The center value positions the grid items in the middle of the container (both vertically and horizontally) 
		.container {
			display: grid;
			height: 300px;
			place-content: center;
		}

	Note: The grid item's total height and width has to be less than the container's height and width for the place-content property to have any effect. 

Grid Item 
--------- 
	A grid container contains one or more grid items.
	All direct child elements of a grid container automatically become grid items. 

Grid Item properties 
-------------------- 

Property			Description
-------------------------------
grid-column-start	Specifies where to start the grid item
grid-column-end		Specifies where to end the grid item
grid-column			A shorthand property for the grid-column-start and the grid-column-end properties

grid-row-start		Specifies where to start the grid item
grid-row-end		Specifies where to end the grid item 
grid-row			A shorthand property for the grid-row-start and the grid-row-end properties 

grid-area			A shorthand property for the grid-row-start, grid-column-start, grid-row-end and the grid-column-end properties

align-self			Aligns the content for a specific grid item along the column axis
justify-self		Aligns the content for a specific grid item along the row axis
place-self			A shorthand property for the align-self and the justify-self properties

CSS Sizing/Spanning Grid Items
------------------------------ 
	A grid item can span over mulitiple columns or rows.

	We can specify where to start and end a grid item by using the following properties:
	------------------------------------------------------------------------------------ 
		grid-column-start - Specifies on which column-line the grid item will start
		grid-column-end - Specifies on which column-line the grid item will end
		grid-column - Shorthand property for grid-column-start and grid-column-end
		grid-row-start - Specifies on which row-line the grid item will start
		grid-row-end - Specifies on which row-line the grid item will end
		grid-row - Shorthand property for grid-row-start and grid-row-end 

	The lines between the columns in a grid are called column-lines, and the lines between the rows in a grid are called row-lines.
	We can refer to line numbers when placing a grid item in a grid container. 

grid-column-start & grid-column-end 
----------------------------------- 
	The grid-column-start property specifies on which column-line the grid item will start.
	The grid-column-end property specifies on which column-line the grid item will end. 

	Let the first grid item start at column-line 1, and end on column-line 3:
	.item1 {
		grid-column-start: 1;
		grid-column-end: 3;
	} 

grid-column 
----------- 
	The grid-column property is a shorthand property for the grid-column-start and the grid-column-end properties. 

	Let the first grid item start at column-line 1, and let it span 2 columns: 
	.item1 {
		grid-column: 1 / span 2;
	} 

grid-row-start & grid-row-end 
----------------------------- 
	The grid-row-start property specifies on which row-line the grid item will start.
	The grid-row-end property specifies on which row-line the grid item will end.

	Let the first grid item start at row-line 1, and end on row-line 3 
	.item1 {
		grid-row-start: 1;
		grid-row-end: 3;
	}

grid-row 
-------- 
	The grid-row property is a shorthand property for the grid-row-start and the grid-row-end properties. 

	Let the first grid item start at row-line 1, and let it span 2 rows:
	.item1 {
		grid-row: 1 / span 2;
	} 

Combine grid-column and grid-row 
-------------------------------- 
	Here we use both the grid-column and grid-row properties to let a grid item span both columns and rows. 

	Let the first grid item start at column-line 1, and let it span 2 columns, 
	also let the first grid item start at row-line 1, and let it span 2 rows:  
	.item1 {
		grid-column: 1 / span 2;
		grid-row: 1 / span 2;
	} 

Naming Grid Items with grid-area 
-------------------------------- 
	The CSS grid-template-areas is a grid container property, and it specifies areas within the grid layout.
	You can name grid items by using the CSS grid-area property, and then reference to the name in the grid-template-areas property.
	Each area is defined within apostrophes. The named grid items in each area is defined inside the apostrophes, separated by a space. 

	Let "Item1" get the name "myHeader", and let it span five columns in a five columns grid layout: 
	.container {
		display: grid;
		grid-template-areas: 'myHeader myHeader myHeader myHeader myHeader';
	}

	.item1 {
		grid-area: myHeader;
	} 

	We can use a period sign (.) to refer to a grid item with no name:

	Let "myHeader" span three columns in a five columns grid layout (a period sign represent an item with no name): 
	.container {
		display: grid;
		grid-template-areas: 'myHeader myHeader myHeader . .';
	}

	.item1 {
		grid-area: myHeader;
	} 

	To define two rows (two areas), define the second row inside another set of apostrophes:

	Let "myHeader" span two columns and two rows:
	.container {
		display: grid;
		grid-template-areas:
			'myHeader myHeader . . .'
			'myHeader myHeader . . .';
	}

	.item1 {
		grid-area: myHeader;
	}

CSS Grid Items - Alignment
-------------------------- 
	justify-self - Specifies the horizontal alignment within a cell
	align-self - Specifies the vertical alignment within a cell
	place-self - Shorthand property for align-self and justify-self

These can also be applied to all grid items in a container using justify-items and align-items on the grid container. 

justify-self 
------------ 
	The justify-self property is used to align a grid item within its grid cell in the inline direction.
	Tip: For pages in English, inline direction is left to right and block direction is top to bottom. 

	Values: auto (default), normal, stretch, start, left, center, end, right 

	Note: For this property to have any alignment effect, the grid item need available space around itself in the inline direction. 
	.item1 {justify-self: right;}
	.item6 {justify-self: center;}

align-self 
---------- 
	The align-self property is used to align a grid item within its grid cell in the block direction. 
	Values: auto (default), normal, stretch, start, end, center

	Note: For this property to have any alignment effect, the grid item need available space around itself in the block direction. 
	.item1 {align-self: start;}
	.item6 {align-self: center;} 

Set the Visual Order of the Grid Items 
-------------------------------------- 
order
----- 
	The CSS order property can be used to define the visual order of the grid items. 
	The first grid item in the HTML source code does not have to appear as the first item in the grid container.

	Set the visual order of the grid items:
	.item1 {order: 3;} 
	.item2 {order: 6;} 
	.item3 {order: 1;} 
	.item4 {order: 2;} 
	.item5 {order: 4;} 
	.item6 {order: 5;} 